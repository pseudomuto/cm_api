# frozen_string_literal: true
source "https://rubygems.org"

# runtime deps should be in gemspec
gemspec

group :development, :test do
  gem "bundler", "~> 1.12"
  gem "rake", "~> 10.0"
  gem "rspec", "~> 3.0"
  gem "rubocop", "~> 0.42"
end
